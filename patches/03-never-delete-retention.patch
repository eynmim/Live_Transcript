diff --git a/src/components/SettingsPage.tsx b/src/components/SettingsPage.tsx
index 4afbf6c..990c32d 100644
--- a/src/components/SettingsPage.tsx
+++ b/src/components/SettingsPage.tsx
@@ -3487,6 +3487,11 @@ EOF`,
                       onChange={(e) => setAudioRetentionDays(parseInt(e.target.value, 10))}
                       className="h-7 rounded border border-border/70 bg-surface-1/80 px-2.5 text-xs font-medium text-foreground shadow-sm backdrop-blur-sm hover:border-border-hover hover:bg-surface-2/70 focus:outline-none focus:ring-2 focus:ring-ring/30 focus:ring-offset-1 transition-colors duration-200"
                     >
+                      <option value={-1}>
+                        {t("settingsPage.privacy.audioRetentionNever", {
+                          defaultValue: "Never delete (keep forever)",
+                        })}
+                      </option>
                       <option value={0}>{t("settingsPage.privacy.audioRetentionDisabled")}</option>
                       <option value={7}>
                         {t("settingsPage.privacy.audioRetentionDays", { count: 7 })}
diff --git a/src/helpers/audioStorage.js b/src/helpers/audioStorage.js
index b4dfd58..20e36ad 100644
--- a/src/helpers/audioStorage.js
+++ b/src/helpers/audioStorage.js
@@ -101,6 +101,16 @@ class AudioStorageManager {
 
   cleanupExpiredAudio(retentionDays, databaseManager) {
     try {
+      // talktype patch: retentionDays < 0 means "never delete" — skip the sweep.
+      if (typeof retentionDays === "number" && retentionDays < 0) {
+        const files = fs.readdirSync(this.audioDir).filter((f) => f.endsWith(".webm"));
+        debugLogger.info(
+          "Audio cleanup skipped (retention=never)",
+          { deleted: 0, kept: files.length, retentionDays },
+          "audio-storage"
+        );
+        return { deleted: 0, kept: files.length };
+      }
       const cutoffMs = Date.now() - retentionDays * 86400000;
       const files = fs.readdirSync(this.audioDir).filter((f) => f.endsWith(".webm"));
       const expiredIds = [];
